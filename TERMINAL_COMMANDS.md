# Terminal command cheatsheet

```bash
# enter package
cd ALIGNMENT_PENDING_DEVKIT_v0.1

# verify local setup and run headless smoke test
./tools/bootstrap.sh

# open Godot editor
./tools/run_game.sh

# start Claude Code
claude
```

In Claude Code, paste `MASTER_PROMPT_CLAUDE.md`. Then instruct: **Execute the next unchecked prompt from `prompts/RUN_ORDER.md` and stop after its completion report.**
