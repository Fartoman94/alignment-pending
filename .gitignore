# Godot 4+
.godot/
.import/
export.cfg
*.translation

# OS
.DS_Store
Thumbs.db

# Build artifacts (regenerated by tools/export_release.sh / CI, never hand-edited)
/dist/

# Supplied prompt/reference bundles (raw source material, not shipped assets —
# only what actually gets copied/generated into game/ and documented in
# docs/legal/ASSET_PROVENANCE.md is committed)
/claude_bundle/
