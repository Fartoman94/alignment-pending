# Implementation status

Update this file after every prompt.

| Prompt | Status | Commit | Notes |
|---|---|---|---|
| P00 | DONE | (pending) | Godot 4.7.2 headless installed for CI. Fixed `smoke_test.gd` Variant-inference parse error (strict typing treats inference warning as error). Bootstrap + headless editor import both clean. No gameplay added. |
| P01 | DONE | (pending) | Added SceneRouter autoload (fade + error-safe ResourceLoader checks, busy-guard). Boot -> MainMenu -> Campaign flow; renamed main.tscn/main.gd to campaign.tscn/campaign.gd. Esc returns to menu from Campaign. Extended smoke_test.gd with scene-resolution, autoload-presence, and router-failure checks (moved autoload checks to `_initialize()`, since autoloads aren't attached yet during `_init()`). |
| P02 | DONE | (pending) | Added SettingsManager autoload (audio bus volumes, UI scale 80-160%, fullscreen/resolution, reduced motion, camera shake) persisted to `user://settings.cfg` with a version field and safe-default fallback on load failure. Added Music/SFX/UI audio buses (`default_bus_layout.tres`). Added Settings screen reachable from MainMenu with Apply/Reset/Back. Wired reduced_motion into SceneRouter fades and Campaign camera lerp (not just stored). Extended smoke_test.gd with clamping and save/load round-trip checks. |
| P03 | DONE | (pending) | Extracted camera into `CameraController` (class_name, own script): WASD + middle-drag pan, Q/E 90° rotation, wheel zoom, F focus-selected (currently resets to office origin; real entity focus deferred to prompts that add selectable entities), bounds-clamped pan. All motion uses exponential (frame-rate-independent) smoothing instead of the old `delta*constant` lerp. Campaign.gd now only owns pause/menu input and HUD. Added a runtime instantiation smoke check (loads main_menu/settings/campaign, adds to tree, processes a frame) catching errors plain existence checks miss. |
| P04 | DONE | (pending) | Added reusable `Hud` scene/script: anchor-driven top resource strip (cash/compute/power/trust/safety debt/date/pause/1x-2x-3x speed), bottom section nav (Build/Staff/Research/Models/Deployments/Company/World — placeholder-only, systems don't exist yet), left objectives panel, right contextual inspector wired to `EventBus.selection_changed`. Replaced campaign.gd's fixed-pixel-position HUD. Added a structural anchor check (not pixel-perfect) proving every strip is anchor-based so 1280x720/1920x1080 both work. Caught and fixed two bugs via the runtime instantiation smoke test: `Hud` class_name needed a fresh editor import pass before headless script runs could resolve it, and BottomBar's button paths were missing a `Margin` path segment. Fixed `tools/bootstrap.sh` to always do a headless editor import/rescan before running the smoke test, so this class-cache trap won't recur for future prompts. |
| P05 | DONE | (pending) | Rewrote SaveManager: 3 rotating autosaves + 3 manual slots under `user://saves/`, temp-file-then-atomic-rename writes validated before ever replacing the real file, SHA-256 checksum envelope (`{save_version, checksum, payload}`), and a migration registry keyed by GameState.SAVE_VERSION (explicit, currently v1, no migrations needed yet). `load_newest_autosave()` walks older rotating slots on checksum/parse failure. Wired real usage: periodic 60s autosave timer + autosave-on-exit in Campaign; MainMenu Continue now loads the newest autosave. Fixed a checksum bug caught by the round-trip test: JSON has no int/float distinction, so hashing pre-round-trip payload (with real ints) never matched the post-parse (all-float) payload on read — fixed by hashing a JSON-normalized copy at write time. |
| P06 | DONE | (pending) | Added `DataValidator` (static, not an autoload): validates JSON content datasets for duplicate IDs, missing required fields, and invalid numeric/enum ranges. Wired to `data/events_seed.json` (category must be one of the 12 documented categories, severity in P0-P3 = [0,3], 2-4 non-empty choices, non-empty title/body). Runs on Boot before routing to MainMenu, pushing one actionable `push_error` per issue; does not hard-block startup since these are content bugs, not corruption. IncidentDefinition fields owned by later systems (prerequisites, weight, cooldown_days, tags) intentionally not required yet. Closes Phase 0 (foundation). |
| P07 | DONE | (pending) | Added `SimClock` autoload: fixed-timestep calendar (day/hour/minute, `EventBus.day_advanced` on day rollover), gated on `active` (Campaign sets true/false on enter/`_exit_tree`) and `GameState.paused`, and named deterministic RNG streams (`rng(name)`/`pick_from(name, items)`) seeded from `(campaign_seed, stream_name)`. Fixed accumulator-based ticking makes calendar progression frame-rate independent (verified by splitting the same elapsed time into 1 vs 370 steps and comparing results). Added `GameState.calendar_day/hour/minute` (persisted) and `GameState.reset_to_defaults()` (randomizes seed, resets calendar/resources) called from MainMenu's New Campaign, which was previously not resetting state between sessions. Corrected HUD speed tiers from 1x/2x/3x (P04's placeholder) to the spec's 1x/2x/4x, and wired the real calendar into the HUD date label (closing a P04 TODO). Deterministic RNG test: same seed + same call sequence produces the same pick sequence. |
| P08 | DONE | (pending) | Added data-driven buildable catalog (`data/buildables.json`: desk/server_rack/safety_lab, cost, data-driven refund_ratio, footprint), `BuildGrid` (8x6 cell grid, occupancy, footprint/rotation math, a reserved mandatory-walkway row that placements can never block), and `BuildController` (ghost preview with green/red valid-invalid feedback, R rotates, click places/sells, re-validates against the authoritative grid even if the cached ghost state says valid). HUD's "Build" section now shows a real palette (one button per catalog entry + Sell + Cancel) instead of a placeholder. Removed the old hardcoded static desk/rack/chair/people scaffold from Campaign — the player now builds everything. Buildings persist in `GameState.buildings` and reload via `BuildController.load_from_state()` on campaign start (continues to survive save/load). Fixed two real bugs the tests caught: (1) DataValidator wrongly required `footprint.w/d` to be GDScript `int` — JSON parsing always yields float, so whole-number floats were being rejected; (2) statically typing test variables as `BuildGrid`/`BuildController` in smoke_test.gd forced Godot to eagerly compile build_controller.gd (which touches GameState) while loading the test script itself, before autoloads exist — fixed by loading those scripts dynamically via `load()` instead of bare class identifiers, same root-cause family as the P04 class-cache issue but at compile-time instead of scene-tree-time. |
| P09 | DONE | (pending) | Added `NavigationRegion3D` over the buildable floor (single static navmesh polygon; buildings get dynamic `NavigationObstacle3D` avoidance instead of navmesh re-baking, so build/sell never needs a bake step). Added `StaffAgent` (idle/moving state machine, `NavigationAgent3D` + RVO avoidance) and `NavCoordinator` (destination reservation: rejects a second agent claiming the same point, with a bounded retry-then-back-off escape hatch instead of blocking forever). Both gate on `GameState.paused`, including the async avoidance velocity callback (a real bug: movement could still land one frame after pausing if only the state-machine tick was gated). No roster/hiring yet (P10) — this prompt only delivers navigation infra, proven by an automated test that spawns 20 synthetic `StaffAgent`s, runs 900 physics ticks, and asserts every agent moved (no stuck/deadlocked agent) and that pausing halts all movement. Real in-game staff will reuse `StaffAgent`/`NavCoordinator` once P10 adds hiring. |
| P10 | DONE | (pending) | Added `data/staff_roles.json` (6 roles: researcher/engineer/data_ops/safety_analyst/product_manager/support_specialist) + `StaffRoleCatalog` loader + DataValidator rules. Added `StaffManager` autoload: deterministic candidate generation (fictional invented names, role, 5 skills, salary) via `SimClock`'s named RNG streams, hire/fire against `GameState.staff` (stable `staff_N` ids from `GameState.next_staff_id`), and daily payroll deducted on `EventBus.day_advanced`. HUD's "Staff" section now shows the real roster + candidate pool with Hire/Fire/Inspect (inspect shows role/salary/morale/fatigue/skills). Campaign spawns/despawns a real `StaffAgent` (from P09's navigation infra) per roster change via a new `EventBus.staff_roster_changed` signal, so hired staff actually walk the office. `values`/`relationships`/`traits`/`assigned_task` are present per DATA_SCHEMA.md but left empty — deep staff modeling (P24) and task assignment (P11) own those. |
| P11 | DONE | (pending) | Added `data/work_tasks.json` (research_sprint/desk/capability, training_run/server_rack/engineering, safety_audit/safety_lab/safety) + `WorkTaskCatalog` + DataValidator rules. Added `TaskManager` autoload: `can_assign`/`assign`/`cancel` enforce compatible building type + nonzero staff skill + no double-booking a workstation; progress accrues on `EventBus.simulation_tick` scaled by the assigned skill (0.5x-1.5x), completing and freeing the workstation once duration is met; orphaned orders (staff fired mid-task) are reconciled via `staff_roster_changed`. Gave every placed building a **stable id** (`GameState.next_building_id`, preserved verbatim across save/load instead of being regenerated) — a real save-compatibility gap this prompt's own feature exposed, since task assignment needs to reference a specific building persistently. `StaffAgent` gained a WORKING state: assigned staff walk to the workstation (reusing P09's nav) and stay until the task ends. HUD staff detail now shows live progress and Assign/Cancel buttons. |
| P12 | DONE | (pending) | Server racks now drive real infrastructure numbers instead of static placeholders: extended `buildables.json`'s server_rack with `compute_units`/`power_draw`/`heat_output`/`operating_cost_per_day` (data-driven, DataValidator-checked). `GameState` gained baseline constants (`BASE_COMPUTE_CAPACITY`=20, `BASE_POWER_CAPACITY`=100, `BASE_POWER_DRAW`=10, `BASE_HEAT_CAPACITY`=60) and `heat_capacity`/`heat_load`/`daily_infrastructure_cost` fields, plus `effective_compute_capacity()` (heat above capacity throttles usable compute via a clamped cooling-efficiency formula, 0.3x-1.0x, rather than anything going negative/invalid). `BuildController._recompute_infrastructure()` derives compute/power/heat/upkeep fully from the current building list after every place/sell/load (never independently mutated, can't drift). Two "over-capacity blocks, never corrupts" gates added: `BuildController` rejects a rack placement that would exceed `power_capacity` (ghost turns red; `try_place()` re-checks authoritatively); `TaskManager.can_assign()` rejects a `training_run` assignment (added `compute_cost` to that task) when there's no free effective-compute headroom, keeping `GameState.compute_used` (now real, synced from active work orders) from ever exceeding capacity. Daily operating cost deducted alongside payroll via `EventBus.day_advanced`. HUD gained a HEAT strip label plus tooltips on Compute/Power/Heat and the Build palette explaining capacity/usage/upkeep. |
| P13 | DONE | (pending) | Added `data/research_nodes.json` (8 nodes across all 6 branches: capability x2, efficiency, interpretability, safety x2, infrastructure, organization) + `ResearchNodeCatalog` + DataValidator rules (including a second-pass check that every prerequisite references a real node id). Added `ResearchManager` autoload: `start(node_id)` gates on prerequisites-met + affordability and commits cash; progress accrues by listening for completed `research_sprint` work orders (extended `TaskManager.assign()`/the `task_assigned`/`task_completed` signals with an optional `target_id` so a work order can reference a specific research node — the same mechanism will carry a model id for P14 training); reaching a node's `duration_minutes` unlocks it and applies a data-driven `unlock_effect` (`compute_bonus` → `GameState.research_compute_bonus`, folded into compute_capacity by BuildController; `safety_debt_delta` / `trust_delta` applied and clamped immediately). This finally gives the P11 placeholder `research_sprint` task real purpose. HUD gained a real "Research" panel (branch/cost/prerequisites/status, Start button) and Staff-detail assignment now auto-targets the lowest-progress active node when assigning research_sprint. Research progress/unlocks persist and were verified across save/load. |
| P14 | DONE | (pending) | Added `data/model_tiers.json` (small/medium/large: cost, duration, 4 base stats) + `ModelTierCatalog` + DataValidator rules. Added `ModelManager` autoload: `start_project(tier_id)` gates on affordability and commits cash; progress accrues via the existing `training_run` task (P11/P12's compute-headroom gating already prevents negative compute) using the same `target_id` mechanism P13 introduced, now referencing a model project instead of a research node; on completion creates a full `ModelArtifact` (all 15 DATA_SCHEMA.md fields) with stats derived from tier base values plus seeded RNG jitter (`SimClock.rng("model_stats")`, deterministic per campaign seed) and an incrementing `generation` number. `cancel_project()` frees any assigned staff/workstation via `TaskManager.cancel()` and removes the project — cost already spent is a sunk cost, never refunded into a negative-adjacent state. HUD gained a "Models" panel (tier list with Start Training, in-progress projects with live progress and Cancel, trained-model roster) and the Staff-detail assignment flow now also auto-targets the active training project. Verified 3 sequential generations train correctly (generation 1/2/3), cancellation is safe both before and during assignment, and insufficient cash cleanly rejects rather than going negative. |
| P15 | DONE | (pending) | Added `EvaluationManager` autoload: the player never sees a model's true stat values directly — only `visible_range()`/`latent_risk_range()`, which narrow as `evals_completed` (the schema's own field, reused rather than adding a new counter) increases. `request_evaluation(model_id)` gates on affordability and depth-not-maxed, commits `$2000` cash, and queues a session; the worker-time cost reuses the existing `safety_audit` task (safety_lab + safety skill) via the same `target_id` mechanism P13/P14 established — giving that P11 placeholder task its real purpose too, same as research_sprint/training_run before it. Regular stats start with a ±25 uncertainty band that shrinks 5/eval to a ±3 floor (never claims perfect certainty); `latent_risk` is fully hidden (`[0,100]`, shown as "Unknown") at depth 0 and only *partially* revealed after — a wide ±40 band shrinking to a ±10 floor, matching the GDD's "hidden attribute, partially revealed by evaluations." Depth caps at 5. Fixed the pre-existing bug where the Models panel displayed models' exact ground-truth stats instead of the uncertain range. |
| P16 | DONE | (pending) | Added `data/deployment_modes.json` (Internal/Beta/Public: `base_user_scale`, `exposure_multiplier`, `rollout_days`, `inference_compute_per_1k_users`) + `DeploymentModeCatalog` (with `next_mode()` for staged Internal→Beta→Public promotion) + DataValidator rules. Added `ReleaseManager` autoload: `deploy()` starts an Internal deployment at 0% rollout; `promote()` advances to the next mode and restarts the ramp (a fresh staged rollout, since the new mode's scale target is larger); `rollback()` cleanly removes it; `set_rate_limit()` is the 0–1 slider. Rollout stage advances daily by `1/rollout_days` via `EventBus.day_advanced`. `current_user_scale`/`total_inference_compute`/`total_incident_exposure` are pure derived functions — directly satisfying "deployment state affects user scale, compute and incident exposure" without needing the users/revenue (P17) or incident (P18) engines that will consume them yet. Refactored `GameState.compute_used` into a derived sum of `training_compute_reserved` (TaskManager) + `inference_compute_used` (ReleaseManager) via `recompute_compute_used()`, so deployed models now genuinely compete with training jobs for the same compute pool established in P12. HUD's Models panel gained Deploy/Promote/Rollback buttons and a real rate-limit HSlider per model. |
| P17 | DONE | (pending) | Added `data/user_segments.json` (hobbyist/developer/business — MVP subset of SYSTEMS_CATALOG's 5; `share_of_market` summing to 1.0, `max_price`, `elasticity`, per-stat weights) + `UserSegmentCatalog` + DataValidator rules (including a cross-record share-sums-to-1.0 check). Added `RevenueManager` autoload: pure aggregate math only — no per-user Node simulation. `compute_breakdown(deployment)` returns a fully traceable per-segment {users, revenue} map plus `total_revenue`/`total_cost`/`net` (`net` always exactly `revenue - cost`). Demand per segment = deployment's `current_user_scale` (from P16) × segment share × a quality score (weighted read of the model's true capability/reliability/safety_confidence) × a price-elasticity factor (`(1 - price/max_price)^elasticity`, so demand falls off as price approaches what a segment will bear). Inference cost/user scales inversely with the model's `cost_efficiency`. Net revenue is applied to cash daily via the existing `EventBus.day_advanced`. HUD gained a price `HSlider` (0–50) and a live users/revenue/cost/net readout per deployed model. |
| P18 | DONE | (pending) | Rewrote `data/events_seed.json` from the 3-entry placeholder schema (flat string choices, ad-hoc `min_scale`) to the full 20-incident condition/weight/cooldown schema: `weight`, `cooldown_days`, `prerequisites` (generic `min_<metric>`/`max_<metric>` against a fixed vocabulary — safety_debt, public_trust, cash, deployed_models_count, staff_count, models_count, calendar_day, compute_used_ratio, total_user_scale (P16), total_incident_exposure (P16)), and `choices[]` as `{id, label, effects}` (effects: cash/public_trust/safety_debt deltas). DataValidator rewritten to match, including cross-checking every prerequisite/effect key against the known vocabularies. Added `IncidentCatalog` + `IncidentManager` autoload: once per `day_advanced`, weighted-random-picks one eligible (prerequisites met + off cooldown) incident via `SimClock.rng("incidents")` (deterministic per seed); triggering a P0 (severity 0) incident force-pauses the game; `resolve()` applies the chosen choice's effects and moves the incident from `pending_incidents` to `incident_history`, recording both a causal state snapshot taken at trigger time and the player's eventual choice — directly satisfying "history records causes/choice." HUD's left panel (previously a static "No active objectives" placeholder since P04) now shows a real incident inbox with a Respond flow into the right panel's choice buttons. |
| P19 | DONE | (pending) | Added `data/communication_actions.json` (Publish Safety Report / Run Marketing Campaign / Issue Clarification) + `CommunicationActionCatalog` + DataValidator rules — including a hard cap (`COMMUNICATION_MAX_TRUST_DELTA` = 10) enforced at the data-validation layer so no single action can ever be authored to erase a severe incident's trust hit in one click. Added `CommunicationManager` autoload: `perform()` costs cash, nudges `public_trust`, and can accrue `GameState.hype_debt` (new field); unaddressed hype debt decays daily by converting into further trust loss (`CORE_LOOP_AND_BALANCE.md`'s "Hype Debt converts to trust loss"), so marketing without substance costs you later. Critically, `CommunicationManager` never touches `GameState.incident_history` — verified by a test that performs every available PR action after a severe incident and asserts the history entry is byte-for-byte unchanged, directly satisfying "PR cannot directly erase severe evidence." `recent_trust_causes()` merges incident + communication contributions into one traceable, day-sorted list, now surfaced as a tooltip on the HUD's TRUST label and in a real "Company" panel (previously a placeholder since P04) showing trust/hype, the communication actions, and a read-only incident history log — "causes visible." |
| P20 | DONE | (pending) | Added `data/rival_doctrines.json` (Safety-First/Speed-First/Open-Tech/Enterprise/Consumer/Research-Prestige, each with `research_pace_multiplier`/`market_pressure_multiplier`) + `RivalDoctrineCatalog` + DataValidator rules. Added `RivalManager` autoload: `generate_rival()` deterministically picks a name (from `WORLD_AND_NARRATIVE.md`'s example pool — original, no real-world mapping) and doctrine via `SimClock`'s seeded RNG, called both at boot and on New Campaign (mirroring the existing `SimClock.reset_rng_streams()` pattern). The rival launches a model generation on a doctrine-paced cycle (`day_advanced`-driven), recording each launch to `rival_launch_history`. `rival_generation()`/`rival_pressure()` are deterministic derived metrics, wired into `IncidentManager`'s condition vocabulary (`min_rival_generation`/`min_rival_pressure`, etc.) — and `board_says_ship`'s prerequisite was updated to `min_rival_generation: 1`, since its own flavor text already referenced a rival launch but had no mechanical tie until now. Verified: 3 sequential launches in order (1, 2, 3), and a rival launch deterministically flips `board_says_ship`'s eligibility. HUD's "World" section (a placeholder since P04) now shows the rival's identity, doctrine, launch progress, market pressure, and launch history. |
| P21 | DONE | (pending) | Added `data/regulator_track.json` — a single fictional regulator (not an array; "one regulator track" for the MVP) with `audit_threshold`, `audit_deadline_days`, a `requirements` checklist, and `full_disclosure`/`minimal_disclosure` effect dicts reusing the incident effects vocabulary — + DataValidator rules requiring a non-empty requirements array ("an audit needs clear requirements"). Extended that shared effects vocabulary with a new `regulatory_pressure` key and wired it into 4 existing incidents (data_leak_scare, evaluation_leak, regulator_inquiry, media_expose) whose flavor text already implied regulatory consequences. Added `RegulatorManager` autoload: `regulatory_pressure` (0-100) accrues daily from deployed user scale (P16/P17) and safety_debt, plus any incident choice's `regulatory_pressure` effect; crossing `audit_threshold` triggers `GameState.active_audit` with the clear requirements list; `resolve_audit()` applies the chosen disclosure's effects and records `audit_history`. HUD's Company panel shows the pressure meter and, when an audit is active, its requirements and the two disclosure buttons. **This closes Phase 1 (vertical slice): P00-P21, 22/48 prompts — every MVP feature from the GDD's "first fun slice" list is now implemented and cross-wired** (office, 6 staff roles, build desks/racks/lab, one research branch, 3 model generations trainable, 20 incidents, pricing/users/compute cost, trust/safety/cash interplay, one rival, one regulator track, save/load). Next up: P22 (vertical slice ending) and Phase 2 (core sim depth). |
| P22 | DONE | (pending) | Added `data/epilogues.json` (3 prototype endings: steady_hand/runaway_growth/grounded_struggle) + `EpilogueCatalog` + DataValidator rules. Added `EndingManager` autoload: the mini-campaign ends deterministically at `calendar_day >= 30`; the epilogue is chosen by a fixed, non-random rule from final `public_trust`/`safety_debt`/`cash`/`models` state (same state → same ending, always). `compute_milestones()` derives 5 achievement flags entirely from state that already exists (`next_building_id`/`next_staff_id`/`next_deployment_id` counters plus `models`/`incident_history` arrays) — zero new tracking infrastructure. Added `scenes/ending.tscn` (epilogue text + milestone checklist + Credits/Main Menu) and `scenes/credits.tscn` (project-team-only roles, explicitly no assistant/AI credits — enforced by an automated test that scans the credits text for "claude"/"anthropic"/"chatgpt"/etc.). `Campaign` autosaves and routes to the ending scene on `EventBus.ending_triggered`, and a loaded save that already ended routes straight there instead of reopening the office. **This closes Phase 1 (vertical slice) end-to-end: P00-P22, 23/48 prompts — a new player can now play from boot through a coherent 30-day mini-campaign to one of 3 endings and see honest credits.** |
| P23 | DONE | (pending) | New `EconomyManager` autoload: rent, legal (per model/deployment/staff — the per-staff term is the anti-snowball scaling), and support costs, plus a single `daily_ledger()` that reuses the exact formulas StaffManager/BuildController/RevenueManager already apply on `day_advanced`, so `forecast_cash_at(days_ahead)` never drifts from the real ledger. Added `GameState.bankruptcy_day` and a 10-day recovery window: going cash-negative starts the clock, recovering clears it, exhausting it while still negative calls the now-public `EndingManager.trigger_ending("bankruptcy")` (new "bankruptcy" epilogue in `epilogues.json`). Split `RevenueManager.total_daily_net()` into `total_daily_revenue()`/`total_daily_cost()` for ledger transparency. HUD Company panel shows the full ledger breakdown, runway estimate, and a bankruptcy warning. Fixed two pre-existing smoke tests whose exact cash-delta assertions didn't yet account for the new autoload's day_advanced deduction. |
| P24 | DONE | (pending) | New `data/staff_traits.json` + `StaffTraitCatalog` (8 traits, each with `skill_deltas`/`morale_delta`/`fatigue_resistance` bounds hard-enforced by `DataValidator` — the acceptance criterion "traits have bounded impact" is a load-time check, not just convention). Candidates get 0-2 random traits. Added `StaffManager.effective_skill()` (base skill + trait deltas + department leadership bonus, clamped 0-100), wired into `TaskManager._on_tick()` so traits/leadership actually affect work-rate. Added `StaffManager.promote()` (one lead per role/department, skill+cash gated, fixed `LEADERSHIP_SKILL_BONUS`). Daily fatigue (rises while assigned, recovers while idle) and morale (decays under sustained high fatigue, recovers when low, nudged by relationship affinity) in `StaffManager._apply_daily_staff_dynamics()`. Lightweight per-department relationships (`{with, affinity}`, clamped ±20). Low-morale staff can resign (`EventBus.staff_resigned`) — reuses `fire()`, so `TaskManager`'s existing orphaned-order reconciliation frees any in-progress task/building automatically, and the candidate pool is independent of the roster, so hiring is never soft-locked. HUD staff detail panel shows traits/lead status and a Promote button. Updated `docs/technical/DATA_SCHEMA.md` StaffMember schema (`is_lead`, trait/relationship shape notes). |
| P25 | DONE | (pending) | New `BoardManager` autoload: `valuation()` (pure function of existing state — revenue, models, trust, safety debt), staged funding rounds (`data/funding_rounds.json` + `FundingRoundCatalog`, raised strictly in order like deployment modes, each gated by a `min_valuation`), and a board-pressure track (`data/board_track.json`, same single-config shape as `RegulatorManager`) that rises with equity given away and low runway. Accepting a round adds cash now and `equity_pct` control loss plus a recurring `obligation_per_day`, which `EconomyManager.daily_ledger()`/`runway_days()` now include as a first-class `investor_obligations` line (acceptance: "funding changes runway and obligations"). Crossing the pressure threshold raises `GameState.active_board_demand`, always resolved via `BoardManager.resolve_demand()` into one of two data-driven effect sets — never an ending (acceptance: "demands create decisions not instant game-over"; verified directly in the smoke test by asserting `ending_id` stays empty after resolution). HUD Company panel shows valuation/control/pressure, a Raise button for the next round, and Yield/Hold buttons for an active demand. Updated `docs/technical/DATA_SCHEMA.md`. |
| P26 | DONE | (pending) | Expanded `GameState.rival` (single Dictionary) into `GameState.rivals` (5 procedural rivals — one per `RivalManager.RIVAL_NAMES`, still fictional/original, each with its own randomized doctrine). This is a real save-schema break, so it's the first actual use of the migration scaffold from P00: `GameState.SAVE_VERSION` bumped 1→2, `SaveManager._migrate_v1_to_v2()` folds an old single `rival` into `rivals[0]` and stamps its launch-history entries with a `rival_id` (verified end-to-end against a hand-built legacy v1 save). Added a bounded catch-up mechanic (`RivalManager.effective_launch_days()`/`catch_up_multiplier()` — a rival behind `leading_generation()` launches faster, capped at 2x base pace) and `RivalManager.market_shares()`, a pure function of every rival's + the player's current scale that always sums to exactly 1.0 (acceptance: "market share sums consistently"). `rival_generation()`/`rival_pressure()` (IncidentManager's condition metrics) now aggregate across the whole field instead of one rival. Smoke test also asserts no rival name matches a real AI company (acceptance: "no rival named after or visually mapped to a real company"). HUD World panel now lists every rival with its own progress/share. |
| P27 | DONE | (pending) | New `LegalManager` autoload: a 0-100 `GameState.legal_exposure` meter that rises from deployed scale + safety debt and is offset (floored at 0) by compliance staffing (`safety_analyst` headcount) — "cases follow player actions" via that same daily accrual. 3 abstract case archetypes (`data/legal_case_types.json` + `LegalCaseTypeCatalog` — no real plaintiffs, purely fictional categories) each with its own exposure threshold, deadline, cooldown, and 3 fully data-driven bounded effect sets (`settle`, `fight`, `injunction`), validated by `DataValidator` (new `validate_legal_case_type_file()`). An open case is guaranteed to resolve within its `case_deadline_days` window one of three ways: the player settles or fights it (`LegalManager.resolve_case()`), a daily `injunction_probability_per_day` roll hits first, or it defaults at the deadline — never left open indefinitely (acceptance: "outcomes bounded and documented", verified by forcing a case through its full deadline window in the smoke test). HUD Company panel shows the exposure meter, compliance headcount, and Settle/Fight buttons for an active case. Purely additive to the save schema (no version bump needed, unlike P26). |
| P28 | DONE | (pending) | New `AgentPermissionManager` autoload + `data/agent_permissions.json`/`AgentPermissionCatalog`: 5 autonomy permissions (coding assistance, support autonomy, research assistance, external tool access, spending authority), grantable per-deployment (`grant()`/`revoke()`, stored in a new `deployment.agent_permissions` array — purely additive, no save-version bump needed). Every permission carries both a `productivity_effects` and a `risk_effects` dict, both required non-empty by `DataValidator` — the acceptance criterion ("each autonomy permission has productivity gain + explicit risk surface") is enforced structurally at data-load time, not just by convention. Effects apply daily and deterministically (no RNG) to whichever meter that permission actually endangers (cash, safety_debt, public_trust, regulatory_pressure, or legal_exposure — reusing P25/P27's effect vocabulary). Models panel shows each deployment's permissions with Grant/Revoke toggles and their productivity/risk description. |
| P29 | DONE | (pending) | New `AutomationManager` autoload ties P28's granted agent permissions to staffing: `automation_pressure()` (total permissions granted across all deployments, a pure function of already-tracked state) scales whichever `WorkforcePolicyCatalog` policy the player has adopted (`data/workforce_policies.json` + `GameState.workforce_policy`, default `"status_quo"` = no effect). 4 policies (status quo, downsize, retrain, augment) with genuinely different cash/morale/trust/safety-debt tradeoffs — `DataValidator` structurally rejects any policy set where one policy is at-least-as-good on every axis and strictly better on at least one, so no policy can be the forced "correct" pick (acceptance: "no forced political conclusion; outcomes depend on policy choices"). Effects apply daily: company-wide cash/trust/safety_debt plus a uniform morale delta across the whole roster. Company panel shows current pressure/policy and lets the player adopt any other policy. |
| P30 | DONE | (pending) | New `DatacenterManager` autoload + `data/datacenter_tiers.json`/`DatacenterTierCatalog`: 4 abstract remote datacenter tiers, purchased strictly in order (same staged-progression pattern as `DeploymentModeCatalog`/`FundingRoundCatalog`), each a single one-time buy adding a large `GameState.datacenter_compute_bonus` and a recurring `datacenter_operating_cost`. `GameState.effective_compute_capacity()` now adds the datacenter bonus on top of the local office's heat-throttled capacity (deliberately not itself subject to the office heat sim — it's abstract, per the prompt). `EconomyManager.daily_ledger()` gained a `datacenters` line. Late-game compute now scales by buying a tier from a management-screen list, never by placing more grid objects (acceptance: "late-game compute scales without rendering thousands of racks" — verified in the smoke test by buying out all 4 tiers and checking the bonus accumulates additively with zero rack placements). Surfaced in the World panel (owned tiers + next tier to purchase). |
| P31 | DONE | (pending) | New `WorldStateManager` autoload + `data/world_variables.json`/`WorldVariableCatalog`: 5 world variables (energy price, chip supply, talent market, public mood, regulation climate) each cycle as `midpoint + amplitude * sin(...)` over `calendar_day`, with a per-campaign phase offset rolled once from `campaign_seed` (`GameState.world_state_phase_offsets`) — deterministic per seed, different-looking per campaign (acceptance: "seeded variation"). `DataValidator` requires `amplitude <= min(midpoint, 100-midpoint)` so a cycle can never leave [0,100]. Each variable wired into one concrete existing system with a named, testable causal link (acceptance: "visible causal links"): energy_price → new `EconomyManager` ledger line; chip_supply → `GameState.effective_compute_capacity()` (via a new `world_compute_availability_multiplier` field, since `GameState` doesn't call other autoloads); talent_market → `StaffManager` candidate salary generation; public_mood → a small daily ambient `public_trust` drift; regulation_climate → `RegulatorManager`'s pressure-accrual rate. World panel lists every variable's live value and effect description. Fixed several pre-existing smoke tests whose exact-value assertions (cash deltas, public_trust deltas) didn't yet account for the two new always-on daily effects (energy cost, public-mood drift). |
| P32 | DONE | (pending) | New `DeploymentPlanManager` autoload + `data/subscription_plans.json`/`SubscriptionPlanCatalog`: 3 subscription tiers (free/pro/enterprise), each a deployment can switch to. `RevenueManager.compute_breakdown()` now enforces the plan's `quota_users` as a hard seat cap (demand beyond it is turned away, not discounted) and adds an enterprise contract's flat daily bonus once signed (`sign_enterprise_contract()`, gated by a reliability floor and a one-time cost). Capacity reservation (`reserve_capacity()`/`release_capacity()`) permanently occupies compute headroom via a new `GameState.recompute_compute_used()` term. New `RevenueManager.predicted_range()` compares current vs. full-rate-limit load/revenue for the pricing UI (acceptance: "pricing UI predicts load/revenue range"). Rate-limit churn: keeping `rate_limit` below 0.3 for 5 consecutive days permanently and irreversibly shrinks a deployment's addressable market by a small, bounded increment, capped so it's never a total wipeout (acceptance: "rate limits can cause churn events"). Models panel surfaces all of it: predicted range, plan switch, contract signing, reservation, and a churn notice. |
| P33 | DONE | (pending) | New `NewsFeedManager` autoload + `data/news_templates.json`/`NewsTemplateCatalog`: 14 offline authored `String.format()` templates across 7 categories (incident resolved, rival launched, funding round accepted, legal case filed, deployment churn, audit triggered, datacenter tier purchased), each triggered by an already-emitted `EventBus` signal (added `incident_resolved` for the last missing hook) and filled from live simulation state — never a network/LLM call anywhere in the project. Picks (outlet, template) go through `SimClock`'s named RNG streams, so the resulting `GameState.news_feed` (capped, oldest dropped) is 100% deterministic given the same seed and event sequence (verified by replaying an identical event sequence twice and diffing the feed). 5 fictional outlet names (`NewsFeedManager.OUTLETS`), checked in the smoke test against real-publication substrings. Surfaced in the World panel as a recent-headlines list. |
| P34 | DONE | (pending) | Expanded `data/events_seed.json` from 20 to 83 original incidents across all 12 categories (6-8 each; every id/title/body verified unique, no near-copies). Severity distribution reviewed and reshaped into a pyramid: 6 crisis (P0), 42 moderate (P1), 28 serious (P2), 7 minor (P3) — rare crises, most weight in the middle. Added a follow-up mechanic (P34's other ask): a choice can carry `follow_up_incident_id` + `follow_up_delay_days`, validated cross-referentially by `DataValidator` (must reference a real, different incident id); `IncidentManager` schedules it into a new `GameState.scheduled_incidents` on resolve and fires it unconditionally (bypassing its own prerequisites — it's an authored continuation, not a resimulated pick) once the delay elapses, preempting that day's normal random pick. Wired 8 real follow-up chains into the new content. Strings remain plain, self-contained JSON fields (title/body/label) with no embedded logic — already localization-ready without any i18n system, which isn't otherwise part of this codebase yet. Fixed the pre-existing "exactly 20 incidents" smoke check to "at least 80". |
| P35 | DONE | (pending) | Expanded `data/research_nodes.json` from 8 to 30 nodes, exactly 5 per branch (capability/efficiency/safety/interpretability/infrastructure/organization). Every branch has its own prerequisite-free root node (2 branches even have 2 roots) — verified structurally in the smoke test (not just eyeballed) — so no branch requires unlocking a different one first; the player can open with any of 9 immediately-available nodes. Cost-per-unlock-amount reviewed for balance: the three compute_bonus branches (capability/efficiency/infrastructure) land within ~3% of each other (566-584 cost per point); safety/interpretability (safety_debt_delta) and organization (trust_delta, the only source of that effect) aren't directly comparable to the others or each other, so no branch can be a strict dominant pick (acceptance: "no single mandatory dominant branch"). A few nodes require prerequisites from two different branches (e.g. `mechanistic_audits`, `safety_case_documentation`, `edge_caching_layer`, `external_advisory_board`) for cross-branch synergy. Fixed the pre-existing "exactly 8 nodes" smoke check to "at least 30". |
| P36 | DONE | (pending) | New `CampaignActManager` autoload + `data/campaign_acts.json`/`CampaignActCatalog`: the 5 acts from `docs/design/WORLD_AND_NARRATIVE.md` ("Garage Intelligence" → "The Benchmark War" → "Infrastructure Company" → "Agents Everywhere" → "Alignment Pending"). `GameState.current_act` is a ratchet (never regresses) advanced purely by capability/scale milestones already tracked elsewhere: first model deployed, first datacenter tier purchased (P30), first autonomy permission granted (P28), and capability or safety debt crossing a threshold — never a calendar timer (acceptance: "campaign can progress without arbitrary timer", verified in the smoke test by ticking 50 days with no milestones met and confirming the act doesn't move). Added a live top-bar act indicator (`hud.tscn` `ActLabel`, tooltip shows the next milestone). |
| P37 | DONE | (pending) | Replaced P22's 3-ending fixed-day-30 prototype with the real ending evaluator the P22 code comment always said would come later. `EndingManager._determine_ending()` is now a priority-ordered, deterministic check of 9 GDD-named endings (`docs/design/GDD.md`/`WORLD_AND_NARRATIVE.md` "Victory/end states"): the secret `simulation_within_simulation` (every current deployment holds every autonomy permission — undiscoverable from the UI) checked first, then `alignment_achieved`, `regulatory_capture`, `open_release_cascade`, `responsible_stewardship`, `market_monarch`, `automation_dividend`, `quiet_acquisition`, with `the_long_pause` as an unconditional fallback so an ending is always eventually reached. Evaluation now starts once `GameState.current_act >= 5` (P36's Act V, milestone-driven) instead of a fixed calendar day — genuinely "no arbitrary timer" now, not just in the acceptance-criteria sense. `bankruptcy` stays a separate, immediate failure path (unchanged). New `GameState.ending_summary`: a frozen snapshot of tracked consequences (final day/cash/trust/safety debt/models/deployments/staff/incidents/market share) built once at trigger time; every epilogue body is a `String.format()` template filled from it (reusing P33's technique) — the epilogue reports facts, never a computed score (acceptance: "summarizes tracked consequences, not moral score", verified by asserting the rendered body has no leftover `{placeholder}` and contains the real day number). Retired the 3 P22 placeholder epilogue ids (steady_hand/runaway_growth/grounded_struggle — non-canonical names, not in the GDD) in favor of the 9 real ones. |
| P38 | DONE | (pending) | New `TutorialManager` autoload + `data/tutorial_steps.json`/`TutorialStepCatalog`: a 7-step contextual sequence (welcome → build compute → hire staff → train a model → evaluate (optional) → deploy → done) covering the exact loop the acceptance criterion asks for. Each step's completion reads an already-tracked GameState fact (a rack placed, `next_staff_id`, `models` non-empty, `evals_completed`, `next_deployment_id`) — genuinely contextual, not a scripted click-through — except the two bookend steps, which complete on explicit dismissal. Every step, including the optional evaluation one, is individually skippable via `dismiss_step()`, and the whole tutorial can be skipped via `skip_all()` (`GameState.tutorial_skipped_all`). Added a live `TutorialBanner` overlay (`hud.tscn`) showing the current step with Skip Step/Skip Tutorial buttons. Also added a `data/glossary_terms.json`/`GlossaryCatalog` (16 terms) and an 8th BottomBar "Glossary" panel. **This closes Phase 3 (content): P00-P38, 39/48 prompts.** |
| P39 | DONE | (pending) | New `ProceduralMeshFactory` (`game/src/render/procedural_mesh_factory.gd`): the one place project code builds primitive 3D geometry (`make_box`/`make_capsule`/`make_cylinder`/`make_material`), so tinting/roughness/transparency stay consistent instead of scattered inline `BoxMesh`/`StandardMaterial3D` calls. Refactored `campaign.gd`'s office shell and `build_controller.gd`'s buildable ghost/placed meshes to route through it — no behavior change, same visuals, now from one named, documented tool instead of ad-hoc primitives. Gave `StaffAgent` an actual visible body for the first time (a tinted capsule torso + head, colored per role via a new `visual_color` field on `data/staff_roles.json`) — previously staff were invisible; real character art/animation is explicitly P40's job, this is just the placeholder-replacement floor. Added an "Asset naming convention" section to `docs/design/ART_ASSET_LIST.md` and 2 new rows to `docs/legal/ASSET_PROVENANCE.md` (acceptance: "provenance ledger updated"). No external asset downloads — everything is either data-driven color values or code-generated primitives at runtime. |
| P40 | DONE | (pending) | Rewrote `StaffAgent._build_visual()` from P39's 2-part placeholder (Torso+Head) into a modular 7-part low-poly body (Torso/Head/LeftArm/RightArm/LeftLeg/RightLeg/Accessory, each its own `MeshInstance3D` via `ProceduralMeshFactory`, tinted by role), plus `_animate_visual(delta)` — cheap per-frame `sin()`-based procedural animation with 3 readable states: a slow idle bob, a walking leg/arm swing while MOVING, and a faster, smaller "focused" arm motion while WORKING, each agent given a random phase offset so a crowd doesn't bob in lockstep. No imported skeleton/AnimationPlayer — code-generated only, per `docs/legal/ASSET_PROVENANCE.md`. Verified 50-agent performance (900 physics ticks in ~15s wall-clock, well under the 20s budget) and, via a real-scene e2e (hiring through the actual `StaffManager`/`campaign.tscn` flow), that all 7 parts exist and IDLE/MOVING/WORKING are each visibly distinct over time. Found and fixed a real cross-test contamination bug while diagnosing an initial "no agents ever move" failure: P36's campaign-act test spikes `safety_debt` to 100 to reach Act V, which can trigger a critical-severity incident that auto-pauses (`IncidentManager`) — that `paused=true` was never reset, silently freezing every `StaffAgent._physics_process()` (which gates on `GameState.paused`) for every test block downstream, including P40's. Fixed by resetting `state.paused`/`state.pending_incidents` in P36's own cleanup block. |
| P41 | DONE | (pending) | New `AudioSynth` (`game/src/audio/audio_synth.gd`): the one place project code generates PCM audio at runtime (`generate_tone`/`generate_pad_loop`), mirroring `ProceduralMeshFactory`'s role for visuals — no imported/downloaded SFX or music anywhere. `generate_tone()` is click-free by construction (linear attack/decay envelope, silent at both ends); `generate_pad_loop()` is a phase-continuous loop (integer chord-ratio partials complete a whole number of cycles over the loop, so the seam never clicks). Added `data/sfx_cues.json` (6 cues: ui_click/build_place/staff_hired/research_unlocked/model_trained/incident_alert) + `SfxCueCatalog` + a `DataValidator` rule that structurally enforces this prompt's "click-free" acceptance criterion (rejects any cue whose attack+decay envelope would overrun its own duration). New `AudioManager` autoload: a data-driven SFX voice pool, an adaptive calm/tense music state machine (`compute_music_state()` — a pure, deterministic function of `GameState.pending_incidents`/`bankruptcy_day`, crossfaded via per-player `volume_db` tweens), and incident ducking (a bus-level, not per-player, attenuation — deliberately kept on a separate axis from the crossfade after finding and fixing a real race condition where both mechanisms fighting over the same player's `volume_db` could permanently strand it at the ducked level). Wired real call sites: `BuildController.try_place()`, `StaffManager.hire()`, `Hud`'s bottom-bar section buttons, and `EventBus.research_unlocked`/`model_created`/`incident_raised`. `SettingsManager`'s pre-existing independent Master/Music/SFX/UI sliders (P02) now drive buses real audio actually plays through. Verified end-to-end in the real campaign scene: hiring/building/an incident all trigger their real cues, ducking measurably attenuates and recovers the Music bus, and the Settings screen's Music slider drives the real bus. |
| P42 | DONE | (pending) | Extended `SettingsManager` with full remapping (`REMAPPABLE_ACTIONS` — 10 actions across camera/pause/build; `rebind_action()`/`current_key_for()`/`default_key_for()`, idempotent `_apply_keybinds()` so InputMap always has exactly one binding per action, persisted in settings.cfg's new `[keybinds]` section) and 4 new accessibility fields: `high_contrast` (a procedurally-built max-contrast `Theme` installed on the root viewport), `colorblind_mode`, `pause_on_incident` ("pause while reading events" — extends the existing critical-severity force-pause in `IncidentManager` to every incident), and `tooltip_delay_sec` (drives the engine's real `gui/timers/tooltip_delay_sec`). Added the redundant non-color signal colorblind mode actually needs: `BuildController`'s ghost preview (green=valid/red=invalid — a color-only signal) gained an "OK"/"X" `Label3D`. Rewrote `scenes/settings.tscn`/`settings_menu.gd` into 3 tabs (Display & Audio / Accessibility / Keybinds), the last built dynamically from `REMAPPABLE_ACTIONS` with a real press-a-key rebind-capture flow. Added an "Accessibility regression checklist" to `docs/production/QA_MATRIX.md` mapping every item from `UI_UX.md`'s Accessibility section to the smoke test block (existing or new) that verifies it — directly satisfying this prompt's "regression checklist passes" acceptance criterion. UI scale and reduced motion (already implemented in P02) were re-verified, not reimplemented. |
| P43 | DONE | (pending) | Full controller support with keyboard parity, built on P42's remap infrastructure: every `REMAPPABLE_ACTIONS` entry can now carry a fixed joypad companion binding (`_apply_keybinds()` adds it alongside the remappable keyboard key) — camera pan on the right stick (left stick/D-pad stay reserved for Godot's built-in `ui_*` menu-focus navigation), rotate on the shoulder bumpers, new `zoom_in`/`zoom_out` actions (keyboard +/- and trigger axes, giving keyboard zoom parity too — previously mouse-wheel only), and face buttons for focus/pause/menu/build-rotate. Added a joypad companion to Godot's own `ui_accept`/`ui_cancel` (shipped keyboard-only by default), so a controller can activate any focused Button at all. Gave `BuildController` a real non-mouse cursor: a new `_cursor_cell` driven by `ui_up/down/left/right` (mouse motion still works, both just update the same cell) plus a new `build_confirm` action (Enter/joypad A) to place/sell — `start_place()`/`start_sell()` release UI focus so the D-pad/left-stick exclusively drives the 3D cursor instead of also reshuffling HUD button focus. `MainMenu`/`Hud`/settings screen now grab sensible initial focus so a controller/keyboard player always has a starting point. Verified end-to-end in the real campaign scene: initial HUD focus, opening the build palette, camera zoom, and a full keyboard/controller build-then-sell round trip, all without a single mouse event. Found and fixed a real bug while testing: the default build cursor position landed on `BuildGrid.ROUTE_ROW` (the reserved walkway, always invalid) — fixed to skip that row. |
| P44 | DONE | (pending) | Profiled the 3 areas `docs/technical/PERFORMANCE_BUDGET.md` calls out, with a bespoke headless profiling harness measuring real wall-clock/CPU time (not synthetic estimates): **(1) 150-agent stress scene** — naive `await physics_frame` timing showed a suspicious ~16.6ms/tick (right at the 60 FPS budget line), which turned out to be a measurement artifact of Godot's default real-time physics pacing in headless mode, not real compute cost; re-measured with `--fixed-fps 600` (removes wall-clock throttling) showed the true cost is ~1.0ms avg / ~2.0ms max per tick for 150 agents — **16x headroom**, no fix required. Still applied one small defensive tuning `PERFORMANCE_BUDGET.md` itself suggests ("stagger path queries"): capped each `StaffAgent`'s `NavigationAgent3D.neighbor_distance`/`max_neighbors` (500m/10 defaults scan far more of a ~14x10 unit office than needed) to 6.0/5. **(2) The `day_advanced` simulation cascade** (20 staff + 5 models) measured ~1.7-2.0ms — within the <4ms-at-1x budget with room to spare; documented that it doesn't fire more often at 4x speed (only sooner), so the <10ms-at-4x budget doesn't apply as a per-tick multiplier. **(3) `Hud._refresh_resource_strip()`** — the one genuine, confirmed bottleneck: with a realistic 300+300-entry incident/communication history, its trust-causes tooltip re-sorts the *entire* history every call, costing ~4ms/call; called unconditionally from `_process()` at 60 Hz, that's ~240ms of CPU per real second on label text alone. Fixed by throttling both HUD refreshes to a 0.15s interval (~6.7 Hz) instead of every frame — an ~8.6x cut, imperceptible to a player, verified in the real running campaign scene (3 label updates over 60 real frames, was 60/60 before). The "500 props" stress target in `PERFORMANCE_BUDGET.md` has no implemented content to profile yet (no decorative prop-instancing system exists in any prior prompt) — noted as a forward-looking budget line, not a current gap. |
| P45 | DONE | (pending) | Localization architecture, deliberately NOT built on Godot's `TranslationServer`/`Translation` classes: a custom `Translation` subclass (the idiomatic Godot approach for a generated pseudo-locale) reproducibly **crashes this project's Godot 4.7.2 headless build at engine shutdown** — reproduced down to a *plain*, un-subclassed `Translation.new()` alone crashing on `quit()`, with zero project code involved (confirmed via a minimal isolated repro before committing to an alternative). Built a self-contained layer instead: `PseudoLocale.pseudo_localize()` (`game/src/localization/pseudo_locale.gd`, pure/deterministic — accents Latin letters, leaves digits/symbols untouched, pads to guarantee >=35% length growth) and `LocalizationManager` autoload (`tr_text()`, `localize_control_tree()` — walks a node's Controls, caches each one's real English source in metadata so re-localizing after a locale switch derives from the original text instead of compounding an already-mangled one). "en" is the real, authored text everywhere (zero indirection/key catalog needed); "es" is an explicitly-labeled *test* locale ("Pseudo-Spanish (QA)" in the UI), not a real translation — a real Spanish translation could easily be shorter than English and wouldn't reliably prove the "+30% string expansion" acceptance criterion the way an algorithmic pseudo-locale does by construction. Wired real coverage: `MainMenu`/`Hud`/`SettingsMenu` localize their static Labels/Buttons on `_ready()`/after Apply; `Hud`'s dynamically-rebuilt resource strip (cash/compute/power/trust/safety-debt/heat/date/act — genuinely "critical gameplay text") routes each generated string through `tr_text()` on every refresh instead. Added a `SettingsManager.locale` field (persisted, a Language dropdown in the Settings screen) — never derived from the OS locale (this dev container's own OS locale is Spanish, which the fix explicitly guards against defaulting to). **Scoped limitation, documented rather than faked:** JSON-authored content strings (incident titles/bodies, buildable/role names, tutorial text, etc. — hundreds of records across ~40 prior prompts) are not yet routed through the pseudo-locale; that's a large, separate content-translation task, not part of this architecture prompt. Verified end-to-end: a real Settings-screen language switch re-localizes its own UI live and persists across reload; a real campaign HUD's dynamic cash label pseudo-localizes on its next throttled refresh while keeping the real digits intact. |
| P46 | TODO | | |
| P47 | TODO | | |
