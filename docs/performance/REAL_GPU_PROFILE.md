# Real GPU profile

Generated by `godot4 --path game --script res://tools/gpu_profile.gd` (real rendering, not `--headless` — see tools/bootstrap.sh's P44 pass for the CPU-only/headless equivalent). Renderer/GPU: `Linux / AMD Radeon 610M (radeonsi, raphael_mendocino, ACO, DRM 3.64, 7.0.0-31-generic)`. This is this development environment's hardware, not necessarily a player's minimum-spec machine — see `docs/production/KNOWN_ISSUES.md`.

- Small (0 staff, 14 buildings): FPS min=7.0 avg=55.5 max=60.0 | frame_time_ms avg=41.52 max=193.80 | draw_calls avg=597 | primitives avg=12086 | static_mem=49.7MB video_mem=23.4MB (360 samples)
- Mid-game (20 staff, 14 buildings): FPS min=59.0 avg=59.8 max=60.0 | frame_time_ms avg=20.76 max=36.92 | draw_calls avg=921 | primitives avg=17228 | static_mem=53.5MB video_mem=23.5MB (360 samples)
- Stress (150 staff, 14 buildings): FPS min=54.0 avg=58.8 max=60.0 | frame_time_ms avg=35.35 max=130.75 | draw_calls avg=3626 | primitives avg=52708 | static_mem=68.3MB video_mem=23.8MB (362 samples)

## Pass history (older entries condensed — full before/after tables for each are in git history if needed)

- **Visual overhaul** (3D asset pack + 2-light setup + closer camera): FPS held/improved; primitives dropped dramatically (real low-poly character models replacing higher-poly procedural capsules).
- **NPC visual rework** (per-instance material variation + idle/seated animation): no measurable regression.
- **Production-kit integration** (garage/office clutter, break room, safety_lab model, 30 character variants): no meaningful regression, draw calls/primitives rose in proportion to new static dressing.
- **City-backdrop window pass** (SubViewport-baked skyline): zero measurable cost — renders once (`UPDATE_ONCE`), never per-frame.
- **Garage vertical-slice recovery** (3 starter desks + chairs/monitors/whiteboard/lounge dressing): no meaningful regression, ~0.3 FPS avg delta.

## Before/after the world+NPC overhaul pass (exterior/neighborhood set + 3 looping traffic vehicles)

Before (same three scenarios, same hardware, prior to `Campaign._build_exterior()` and `TrafficVehicle`):

| Scenario | Before FPS avg | After FPS avg | Before draw calls | After draw calls | Before primitives | After primitives |
|---|---|---|---|---|---|---|
| Small | 56.0 | 55.5 | 473 | 597 | 9,566 | 12,086 |
| Mid-game (20 staff) | 59.7 | 59.8 | 819 | 921 | 15,200 | 17,228 |
| Stress (150 staff) | 58.7 | 58.8 | 3,315 | 3,626 | 54,531 | 52,708 |

No meaningful regression — every scenario still at or near 60 FPS. This is the first pass this session with real *per-frame* cost (3 `TrafficVehicle`s moving every physics tick, not one-time set dressing like every prior pass) and it's still within noise — a straight-line ping-pong mover with no pathfinding/avoidance is cheap by design, exactly the "no hace falta tránsito complejo de simulación" scope the brief itself called for.
